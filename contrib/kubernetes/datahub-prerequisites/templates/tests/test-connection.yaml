apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datahub-prerequisites.fullname" . }}-test-connection"
  labels:
    {{- include "datahub-prerequisites.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datahub-prerequisites.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
